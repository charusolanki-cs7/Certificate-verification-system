const express = require('express');
const router = express.Router();
// Student-specific routes can be added here if needed
// For now, students primarily use the certificate routes

module.exports = router;
